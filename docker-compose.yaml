---
services:
  ariang:
    build: .
    container_name: ariang
    volumes:
      - ./downloads:/downloads
    restart: unless-stopped
    networks:
      ariang:
networks:
  ariang:
    name: ariang
